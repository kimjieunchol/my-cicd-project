apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sk066-frontend
  namespace: argocd
spec:
  project: default
  
  source:
    repoURL: https://github.com/kimjieunchol/my-cicd-project.git
    targetRevision: main
    path: frontend/k8s
  
  destination:
    server: https://kubernetes.default.svc
    namespace: skala-practice
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m